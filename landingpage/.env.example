# Copy to .env and fill in before deploying
PORT=3000
ADMIN_USER=admin
ADMIN_PASS=your-secure-password-here

# PostgreSQL — Railway sets DATABASE_URL automatically.
# For local dev, copy the connection string from your Railway project dashboard.
DATABASE_URL=postgresql://user:password@localhost:5432/lorebound
